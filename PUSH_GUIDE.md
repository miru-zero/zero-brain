# Push จากเครื่องป๊า (repo นี้ถูกสร้างผ่าน MCP — เนื้อโค้ดเต็มอยู่ใน project/ ที่ local)

```
cd <project>
git remote add origin https://github.com/miru-zero/central-brain.git
git push -u origin main
```
